export function pow2abs(a: number, b: number): number;

export function distance(touches: { pageX: number, pageY: number }[]): number | false;

export function toDeg(rad: number): number;

export function angle(touches: { pageX: number, pageY: number }[]): number;
